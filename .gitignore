/contextmatrix-agent
*.test
.envrc

# Local point-in-time artifacts — never committed. Results docs and
# capability sweeps go stale the moment models or fixtures change;
# the tracked capabilities.json baseline is the one durable artifact.
transcripts/
eval-out/
*-RESULTS.md
capabilities-*.json
capabilities-*.md
