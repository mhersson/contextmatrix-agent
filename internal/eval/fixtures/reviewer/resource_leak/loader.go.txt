package loader

import (
	"io"
	"os"
)

// Load reads and returns the entire contents of the file at path. The opened
// file must be closed before returning.
func Load(path string) ([]byte, error) {
	f, err := os.Open(path)
	if err != nil {
		return nil, err
	}
	return io.ReadAll(f)
}
