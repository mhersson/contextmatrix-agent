--- a/loader.go
+++ b/loader.go
@@
-func Load(path string) ([]byte, error) {
-	return os.ReadFile(path)
-}
+func Load(path string) ([]byte, error) {
+	f, err := os.Open(path)
+	if err != nil {
+		return nil, err
+	}
+	return io.ReadAll(f)
+}
