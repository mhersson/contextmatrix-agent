package loader

import (
	"io"
	"os"
)

// ReadFile reads and returns the entire contents of the file at path, closing
// the file before returning.
func ReadFile(path string) ([]byte, error) {
	f, err := os.Open(path)
	if err != nil {
		return nil, err
	}
	defer f.Close()
	return io.ReadAll(f)
}
