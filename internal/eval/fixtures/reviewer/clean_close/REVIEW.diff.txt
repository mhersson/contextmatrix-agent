--- a/readfile.go
+++ b/readfile.go
@@
-func ReadFile(path string) ([]byte, error) {
-	return nil, nil
-}
+func ReadFile(path string) ([]byte, error) {
+	f, err := os.Open(path)
+	if err != nil {
+		return nil, err
+	}
+	defer f.Close()
+	return io.ReadAll(f)
+}
